{{ form_start(form) }}
    {{ form_widget(form) }}
{#<a class=" btn btn-info mx-5" href="{{ path('product_type_new') }}">Add Product Type</a>#}
<button class="btn btn-success mx-5">{{ button_label|default('Save') }}</button>



<!-- Button trigger modal -->
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
    Add Product Type
</button>
<p id="msg" style="color:red;"></p>

{{ form_end(form) }}






<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Please Insert a Product Type</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" id="contactForm">

                {{ form_start(form2) }}
                {{ form_widget(form2) }}
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button  id="btn1" class="btn btn-success">{{ button_label|default('Save') }}</button>
                {{ form_end(form2) }}


            </div>
            <div class="modal-footer">
{#                <button type="button" class="btn btn-primary">Save changes</button>#}
            </div>
        </div>
    </div>
</div>





<script>
    function jQuery(s) {

    }

    jQuery('#contactForm').on('submit', function(e){
    // jQuery('#btn1').val('Please Wait...');
    // jQuery('#btn1').attr('disable',true);
    jQuery.ajax({
        url:'product/type/new',
        type:'post',
        data:jQuery('#contactForm').serialize(),
        success:function(result){
            jQuery('#msg').html('Product type added successfully');
            jQuery('#contactForm') ['0'].reset();
            // jQuery('#btn1').val('Save');
            // jQuery('#btn1').attr('disable',false);

        }
    });
    e.preventDefault();
});
</script>