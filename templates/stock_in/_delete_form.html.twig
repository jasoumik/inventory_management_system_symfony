<form method="post" action="{{ path('stock_in_delete', {'id': stock_in.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_in.id) }}">
    <button class="btn btn-danger">Delete</button>
    <br>
    <a href="{{ path('product_index') }}">Product Home</a>
</form>
